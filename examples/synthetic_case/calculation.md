The case uses B=8 and physical costs b_p1=5, b_p2=4, b_p3=4, b_p4=4, b_p5=4. Candidate c1 is shared physical record p1 and contains two explicit structured claims, one for revenue supporting E1 and one for operating cash flow supporting E2. The logical allocation matrix selects p1 in both rows, while the physical inclusion vector includes p1 once. Budget used is 5, not 10. Expansion is not triggered because the initial pool resolves both slots.
